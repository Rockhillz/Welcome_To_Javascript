// To count characters in a string 
// 1. Take in string either through prompt or input in a function 
// 2. use the .length to get the number of characters in a string 

function counting() {
    inputt = prompt("Input strings");
    lengthChar = inputt.length
    console.log(lengthChar)
}

counting()